# AI usage

I used an AI coding assistant to inspect the supplied PDF, scaffold Python and
Markdown files, find the current direct FLORES download after the legacy short
link failed, and draft calculations/prose. I independently checked the
assignment inputs, ran every command reported here, inspected the CSV arithmetic
and tokenizer outputs, and revised claims to include only measured deltas.

The assistant's first FLORES downloader used the short URL and failed because
the URL now serves HTML. That was a useful reminder that generated setup code
is not evidence; I tested it, diagnosed the failure, switched to the official
direct archive, and recorded the source checksum. I understand and can rerun
the Python metrics and re-derive the KV and goodput calculations.
